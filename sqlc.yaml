version: "2"
sql:
  - engine: "postgresql"
    schema: "schema/schema.sql"
    queries: "pkg/persistence/queries"
    gen:
      go:
        package: "db"
        out: "pkg/persistence/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
